import { AuthenticatedRequest } from "../../../middleware/authenticationMiddleware";

export const changePassaword = async (
  req: AuthenticatedRequest,
  res: Response
) => {
  try {
  } catch (error) {}
};
